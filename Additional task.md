# АНАЛИЗ ИЗОБРАЖЕНИЯ ДВЕРИ MIO

Перед написанием задания — детальный визуальный анализ того что вижу на фото:

**Полотно:**
- Глухое (без остекления)
- Поверхность матовая, покрыта белой/молочной эмалью (RAL 9010 или близкий)
- По периметру полотна — фрезеровка в виде **трёх параллельных прямоугольных канавок** разной глубины, создающих эффект ступенчатой рамки-филёнки
- Центр полотна — гладкая плоскость без декора
- Фрезеровка не доходит до низа полотна — вертикальные линии прерываются примерно на 1/3 высоты снизу, что создаёт асимметричный дизайнерский акцент
- Ручка — хромированная, нажимная, минималистичная, горизонтального типа, установлена без подиума

**Наличники:**
- На изображении наличники не показаны (дверь представлена без коробки)

**Стиль:**
- Неоклассика / современная классика — геометрическая филёнка без завитков и лепнины
- Подходит: неоклассика, современная классика, transitional, light modern, скандинавский минимализм с классическими акцентами

---

# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Доработка генерации AI-описания: визуальный анализ + стилевая классификация

---

## ПРОБЛЕМА

Текущий результат в `ai_semantic_description` — маркетинговый текст без визуального содержания:

```
"Дверь Mio 1 asm – это образец лаконичного дизайна и безупречного качества 
от фабрики ESTET. Выполнена в стиле минимализм, она станет прекрасным 
дополнением к любому современному интерьеру. Покрытие эмалью обеспечивает 
долговечность..."
```

**Что не так:**
- ИИ не видит изображение — описывает по названию
- Нет информации о фрезеровках, филёнках, профиле
- Нет подбора стилей интерьера где применима дверь
- Поле `style` в БД содержит одно слово "минимализм" — этого недостаточно

**Требуемый результат** (составлен на основе реального анализа фото MIO):

```json
{
  "panel_description": "Глухое полотно без остекления. Поверхность покрыта 
    матовой эмалью молочно-белого тона. По периметру полотна выполнена 
    фрезеровка в виде трёх параллельных прямоугольных канавок, образующих 
    ступенчатую рамку-филёнку. Характерная особенность: вертикальные линии 
    фрезеровки не замкнуты снизу — они прерываются на высоте примерно 1/3 
    от низа полотна, создавая намеренно незавершённый дизайнерский акцент. 
    Центральная часть полотна — гладкая плоская поверхность без дополнительного 
    декора. Ручка хромированная нажимная, минималистичного горизонтального 
    профиля, установлена непосредственно в полотно без подиума.",
    
  "casing_description": "Наличники на изображении не представлены.",
  
  "style_analysis": {
    "door_style": "Неоклассика с элементами современной классики. Геометрическая 
      филёнка без исторических орнаментов и лепнины придаёт двери сдержанную 
      элегантность. Асимметричное завершение фрезеровки — дизайнерский приём 
      характерный для transitional-стиля.",
    "compatible_interior_styles": [
      "Неоклассика",
      "Современная классика",
      "Transitional (переходный стиль)",
      "Светлый минимализм с классическими акцентами",
      "Скандинавский стиль",
      "Французский прованс в современной интерпретации"
    ],
    "incompatible_styles": [
      "Лофт",
      "Хай-тек",
      "Брутализм",
      "Рустик"
    ]
  },
  
  "full_description": "Полотно двери Mio 1 выполнено в технике геометрической 
    фрезеровки: по периметру проходят три параллельные прямоугольные канавки, 
    формирующие ступенчатую рамку-филёнку. Отличительная черта модели — 
    вертикальные линии фрезеровки намеренно не замкнуты снизу, что создаёт 
    лёгкую асимметрию и современный характер. Поверхность покрыта матовой 
    эмалью молочно-белого тона, центр полотна остаётся гладким. Дверь органично 
    вписывается в интерьеры в стиле неоклассики, современной классики и 
    transitional — там где нужна сдержанная элегантность без исторической 
    перегруженности."
}
```

---

## ФАЙЛ ДЛЯ ИЗМЕНЕНИЯ

```
services/parser/app/analyzer/description_generator.py
```

---

## ЗАДАЧА 1: Новый промпт `_build_visual_description_prompt`

Полностью заменить существующий промпт:

```python
def _build_visual_description_prompt(
    self,
    product_data: dict,
    collection_info: dict = None,
) -> str:
    """
    Промпт для визуального анализа изображения двери.
    
    Результат содержит:
    1. Визуальное описание полотна (фрезеровки, филёнки, поверхность)
    2. Описание наличников если видны
    3. Анализ стиля и подбор совместимых стилей интерьера
    4. Итоговый связный текст для поля ai_semantic_description
    """
    product_name = product_data.get("name", "")

    collection_context = ""
    if collection_info:
        if collection_info.get("description"):
            collection_context += f"""
ОПИСАНИЕ КОЛЛЕКЦИИ (используй как контекст и терминологический ориентир):
{collection_info['description']}
"""
        if collection_info.get("features"):
            features_text = "\n".join(
                f"  - {f}" for f in collection_info["features"]
            )
            collection_context += f"""
ЗАЯВЛЕННЫЕ ОСОБЕННОСТИ КОЛЛЕКЦИИ:
{features_text}
"""

    prompt = f"""
Ты эксперт по межкомнатным дверям и дизайну интерьеров.
Твоя задача — проанализировать изображение двери и создать структурированное описание.

ПРОДУКТ: {product_name}
{collection_context}

═══════════════════════════════════════════════════
ЧАСТЬ 1 — ВИЗУАЛЬНЫЙ АНАЛИЗ ДВЕРИ
═══════════════════════════════════════════════════

Внимательно рассмотри изображение. Опиши ТОЛЬКО ТО ЧТО ВИДИШЬ.
Не придумывай детали которых нет на фото.

Для поля "panel_description" опиши:

А. ПОЛОТНО:
   - Тип поверхности: гладкое / с фрезеровками / филенчатое / комбинированное?
   - Если есть ФРЕЗЕРОВКИ:
       * Сколько линий/канавок?
       * Их форма: прямоугольные / скруглённые / V-образные?
       * Расположение: по периметру / вертикальные / горизонтальные / диагональные?
       * Замкнуты ли линии по всему периметру или прерываются?
       * Глубина: поверхностные / выраженные / глубокие?
   - Если есть ФИЛЁНКИ:
       * Количество и расположение (верхняя/нижняя/левая/правая)
       * Форма: прямоугольные / арочные?
       * Тип: врезные / накладные?
   - ОСТЕКЛЕНИЕ: есть / нет? Если есть — форма, размер, расположение
   - ТЕКСТУРА ПОВЕРХНОСТИ: матовая / глянцевая / сатиновая / с текстурой под дерево?
   - ЦВЕТ: опиши точно (молочно-белый / бежевый / серый и т.д.)
   - РУЧКА: тип, материал, форма, наличие подиума

Б. НАЛИЧНИКИ (если видны на изображении):
   - Профиль: плоский / ступенчатый / фигурный / классический с калёвкой?
   - Ширина: узкий (~50мм) / стандартный (~70мм) / широкий (>90мм)?
   - Торец: прямой / скруглённый / с фаской?
   - Стыковка в углах: под 45° / с угловым элементом?

В. ДОПОЛНИТЕЛЬНЫЕ ЭЛЕМЕНТЫ:
   - Подиум для ручки: есть / нет, если есть — форма и размер
   - Молдинги или декоративные накладки
   - Любые другие заметные элементы

═══════════════════════════════════════════════════
ЧАСТЬ 2 — СТИЛЕВОЙ АНАЛИЗ
═══════════════════════════════════════════════════

На основе УВИДЕННОГО на изображении (форма фрезеровок, наличие/отсутствие 
декора, пропорции, тип поверхности):

Для поля "door_style" определи:
   - В каком стиле выполнена дверь?
   - Какие визуальные признаки на это указывают?
   - Пример: "Неоклассика с элементами transitional — геометрическая 
     ступенчатая рамка без исторических орнаментов, асимметричное 
     завершение фрезеровки как современный дизайнерский акцент"

Для поля "compatible_interior_styles" подбери стили интерьера:
   КРИТЕРИИ ПОДБОРА:
   - Стиль двери должен соответствовать характеру интерьера
   - Учитывай: наличие/отсутствие декора, геометрию линий, 
     цвет и фактуру поверхности
   
   СПИСОК СТИЛЕЙ ДЛЯ АНАЛИЗА (выбери подходящие):
   Неоклассика, Современная классика, Transitional, Минимализм,
   Скандинавский стиль, Контемпорари, Ар-деко, Прованс,
   Французский стиль, Американский стиль, Эклектика,
   Лофт, Хай-тек, Индустриальный, Японский минимализм,
   Рустик, Шале, Кантри, Средиземноморский

Для поля "incompatible_styles" укажи стили где дверь будет неуместна
и объясни кратко почему.

═══════════════════════════════════════════════════
ФОРМАТ ОТВЕТА
═══════════════════════════════════════════════════

Верни строго валидный JSON без markdown обёртки:

{{
  "panel_description": "детальное описание полотна двери на основе изображения",
  "casing_description": "описание наличников или 'Наличники на изображении не представлены'",
  "additional_elements": "дополнительные видимые элементы или пустая строка",
  "style_analysis": {{
    "door_style": "стиль в котором выполнена дверь с обоснованием",
    "compatible_interior_styles": ["Стиль 1", "Стиль 2", "Стиль 3"],
    "incompatible_styles": ["Стиль А — причина", "Стиль Б — причина"]
  }},
  "full_description": "связный читаемый текст 4-6 предложений объединяющий визуальное описание и стилевую характеристику"
}}

ТРЕБОВАНИЯ К full_description:
- Первые 2-3 предложения: что видно на двери (полотно, фрезеровки, поверхность)
- Последние 1-2 предложения: в каких интерьерах уместна
- Конкретно и точно: не "красивая дверь" а "три параллельные фрезеровки по периметру"
- Без упоминания бренда, цены, доставки
- Без фраз "на изображении", "я вижу", "судя по фото"
"""
    return prompt
```

---

## ЗАДАЧА 2: Метод `generate_from_image`

```python
async def generate_from_image(
    self,
    image_bytes: bytes,
    product_data: dict,
    collection_info: dict = None,
) -> dict:
    """
    Генерирует визуальное описание и стилевой анализ двери.

    Args:
        image_bytes:     Байты изображения продукта (webp/jpg/png)
        product_data:    Метаданные продукта из scraper
        collection_info: Данные коллекции как контекст (опционально)

    Returns:
        dict: {
            "panel_description":   str,
            "casing_description":  str,
            "additional_elements": str,
            "style_analysis": {
                "door_style":                str,
                "compatible_interior_styles": List[str],
                "incompatible_styles":        List[str]
            },
            "full_description": str   ← записывается в ai_semantic_description
        }
        
    Возвращает {} если изображение не передано или произошла ошибка.
    """
    if not image_bytes:
        logger.warning(
            f"⚠️ Нет изображения для '{product_data.get('name')}'. "
            f"Пропускаем визуальный анализ."
        )
        return {}

    prompt = self._build_visual_description_prompt(
        product_data=product_data,
        collection_info=collection_info,
    )

    try:
        response_text = await self.client.generate_with_image(
            prompt=prompt,
            image_bytes=image_bytes,
            image_mime_type=self._detect_mime_type(image_bytes),
        )

        result = self._parse_json_response(response_text)

        # Валидация результата
        full_desc = result.get("full_description", "")
        style_analysis = result.get("style_analysis", {})
        compatible_styles = style_analysis.get("compatible_interior_styles", [])

        if len(full_desc) < 100:
            logger.warning(
                f"⚠️ Слишком короткое описание ({len(full_desc)} симв.) "
                f"для '{product_data.get('name')}'. Возможно ИИ не увидел изображение."
            )

        if not compatible_styles:
            logger.warning(
                f"⚠️ Не получены совместимые стили для '{product_data.get('name')}'"
            )

        logger.info(
            f"✅ Визуальный анализ завершён: '{product_data.get('name')}'\n"
            f"   Описание: {len(full_desc)} символов\n"
            f"   Стиль двери: {style_analysis.get('door_style', 'не определён')[:80]}\n"
            f"   Совместимых стилей: {len(compatible_styles)}"
        )

        return result

    except Exception as e:
        logger.error(
            f"❌ Ошибка визуального анализа '{product_data.get('name')}': {e}",
            exc_info=True
        )
        return {}


def _detect_mime_type(self, image_bytes: bytes) -> str:
    """
    Определяет MIME-тип изображения по сигнатуре байт.
    """
    if image_bytes[:4] == b'RIFF' and image_bytes[8:12] == b'WEBP':
        return "image/webp"
    elif image_bytes[:3] == b'\xff\xd8\xff':
        return "image/jpeg"
    elif image_bytes[:8] == b'\x89PNG\r\n\x1a\n':
        return "image/png"
    else:
        # Fallback — большинство изображений на сайте webp
        return "image/webp"


def _parse_json_response(self, response_text: str) -> dict:
    """
    Парсит JSON из ответа Gemini.
    Обрабатывает markdown обёртку ```json ... ```
    """
    import json
    import re

    if not response_text:
        return {}

    clean = response_text.strip()

    # Убираем markdown обёртку
    json_match = re.search(r"```(?:json)?\s*([\s\S]*?)\s*```", clean)
    if json_match:
        clean = json_match.group(1).strip()

    try:
        return json.loads(clean)
    except json.JSONDecodeError as e:
        logger.warning(f"⚠️ Не валидный JSON в ответе AI: {e}")
        logger.debug(f"Ответ AI: {response_text[:500]}")
        # Fallback — сохраняем текст как full_description
        return {
            "panel_description": "",
            "casing_description": "",
            "additional_elements": "",
            "style_analysis": {
                "door_style": "",
                "compatible_interior_styles": [],
                "incompatible_styles": [],
            },
            "full_description": response_text.strip(),
        }
```

---

## ЗАДАЧА 3: Сохранение стилей в БД

### 3.1 Изменение в `run_parser.py`

В функции `_parse_single_product` после получения AI результата:

```python
if ai_result.get("full_description"):
    product_data["ai_semantic_description"] = ai_result["full_description"]
    stats["ai_descriptions_generated"] += 1

# Сохраняем стилевой анализ
style_analysis = ai_result.get("style_analysis", {})

if style_analysis.get("door_style"):
    # Перезаписываем поле style более точным значением от AI
    product_data["style"] = style_analysis["door_style"]

if style_analysis.get("compatible_interior_styles"):
    # Сохраняем список совместимых стилей в special_features
    # или в отдельное поле если оно есть в схеме БД
    compatible = style_analysis["compatible_interior_styles"]
    
    # Добавляем к existing special_features
    existing_features = product_data.get("special_features", [])
    style_features = [
        f"Стиль интерьера: {s}" for s in compatible
    ]
    product_data["special_features"] = existing_features + style_features

logger.info(
    f"   🎨 Стиль двери: {style_analysis.get('door_style', 'не определён')[:60]}\n"
    f"   🏠 Совместимые стили: "
    f"{', '.join(style_analysis.get('compatible_interior_styles', []))}"
)
```

### 3.2 Опционально: новое поле в таблице `products`

Если схема БД позволяет добавить поле:

```sql
-- Добавить поле для хранения совместимых стилей интерьера
ALTER TABLE products 
ADD COLUMN IF NOT EXISTS compatible_styles JSONB DEFAULT '[]'::jsonb;

-- Добавить поле для детального стилевого описания
ALTER TABLE products
ADD COLUMN IF NOT EXISTS door_style_description TEXT DEFAULT '';
```

И соответствующая миграция Alembic:

```python
def upgrade():
    op.add_column(
        'products',
        sa.Column('compatible_styles', postgresql.JSONB, 
                  server_default='[]', nullable=True)
    )
    op.add_column(
        'products',
        sa.Column('door_style_description', sa.Text, 
                  server_default='', nullable=True)
    )

def downgrade():
    op.drop_column('products', 'compatible_styles')
    op.drop_column('products', 'door_style_description')
```

---

## ЭТАЛОННЫЙ РЕЗУЛЬТАТ

На основе реального анализа изображения MIO — именно такой результат должен генерировать код:

```json
{
  "panel_description": "Глухое полотно без остекления. Поверхность покрыта матовой эмалью молочно-белого тона. По периметру полотна выполнена фрезеровка в виде трёх параллельных прямоугольных канавок нарастающей глубины, образующих ступенчатую рамку-филёнку. Характерная особенность модели: вертикальные линии фрезеровки не замкнуты снизу — они прерываются примерно на высоте 1/3 от нижнего края полотна, создавая намеренно незавершённый асимметричный акцент. Центральная часть полотна — ровная гладкая плоскость без дополнительного декора. Ручка нажимная хромированная, горизонтального минималистичного профиля, установлена непосредственно в полотно без подиума.",

  "casing_description": "Наличники на изображении не представлены.",

  "additional_elements": "Подиум для ручки отсутствует. Петли скрытые либо не попадают в кадр.",

  "style_analysis": {
    "door_style": "Неоклассика в современной интерпретации (transitional). Геометрическая ступенчатая рамка-филёнка отсылает к классической архитектуре, однако полное отсутствие исторических орнаментов, лепнины и скруглений делает дверь современной. Намеренно незамкнутая фрезеровка — дизайнерский приём характерный для переходного стиля между классикой и минимализмом.",
    "compatible_interior_styles": [
      "Неоклассика",
      "Современная классика",
      "Transitional (переходный стиль)",
      "Контемпорари",
      "Скандинавский стиль",
      "Светлый минимализм с классическими акцентами",
      "Французский стиль в современной интерпретации",
      "Американский стиль"
    ],
    "incompatible_styles": [
      "Лофт — грубая фактура и индустриальный характер лофта контрастирует с изящной геометрией двери",
      "Хай-тек — требует полностью гладких поверхностей без рамочных элементов",
      "Брутализм — несовместим с белым цветом и изящными фрезеровками",
      "Рустик / Шале — натуральные фактуры дерева не сочетаются с гладкой эмалью"
    ]
  },

  "full_description": "Полотно двери Mio 1 украшено фрезеровкой в виде трёх параллельных прямоугольных канавок по периметру, формирующих ступенчатую геометрическую рамку. Отличительная черта модели — вертикальные линии фрезеровки не замкнуты снизу и прерываются на 1/3 высоты от пола, создавая лёгкую асимметрию и современный характер. Поверхность покрыта матовой эмалью молочно-белого тона, центр полотна остаётся гладким. Дверь выполнена в стиле неоклассики в современной интерпретации и органично вписывается в интерьеры в стиле transitional, современной классики, контемпорари и скандинавского стиля — там где нужна сдержанная элегантность без исторической перегруженности."
}
```

---

## ЧЕКЛИСТ ДЛЯ QWEN CODE АГЕНТА

```
Файл: description_generator.py

  ✅ Заменить _build_visual_description_prompt
     — промпт в двух частях: визуальный анализ + стилевой анализ
     — инструкция описывать фрезеровки, филёнки, замкнутость линий
     — список стилей интерьера для классификации
     — требование JSON с полем style_analysis

  ✅ Заменить generate_from_image
     — передача image_bytes в Gemini Vision
     — валидация: проверка длины full_description (>100 символов)
     — валидация: проверка наличия compatible_interior_styles
     — логирование стиля и количества совместимых стилей

  ✅ Добавить _detect_mime_type
     — определение по сигнатуре байт (webp/jpg/png)

  ✅ Заменить _parse_json_response
     — снятие markdown обёртки ```json```
     — fallback при невалидном JSON

Файл: run_parser.py

  ✅ После получения ai_result сохранять:
     — ai_result["full_description"] → product_data["ai_semantic_description"]
     — style_analysis["door_style"]  → product_data["style"]
     — compatible_styles             → product_data["special_features"] или 
                                       product_data["compatible_styles"]
  ✅ Передавать collection_info в _parse_single_product
  ✅ Вызывать generate_from_image с collection_info

БД (опционально):
  ✅ Добавить поле compatible_styles JSONB
  ✅ Добавить поле door_style_description TEXT
  ✅ Создать миграцию Alembic
```