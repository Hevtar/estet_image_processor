version: '3.9'

services:
  postgres:
    image: pgvector/pgvector:pg15
    container_name: estet_postgres
    environment:
      POSTGRES_USER: estet
      POSTGRES_PASSWORD: estet_dev_password
      POSTGRES_DB: estet_platform
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U estet"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Vision API (раскомментируйте для запуска через Docker)
  # vision-api:
  #   build:
  #     context: ./services/vision-api
  #     dockerfile: Dockerfile
  #   container_name: estet_vision_api
  #   env_file:
  #     - .env
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - ./services/vision-api:/app
  #   command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # Parser (раскомментируйте для запуска через Docker)
  # parser:
  #   build:
  #     context: ./services/parser
  #     dockerfile: Dockerfile
  #   container_name: estet_parser
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./services/parser/data:/app/data
  #     - ./services/parser/logs:/app/logs
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  postgres_data:
